CODEX.register({
  id:'sql', name:'SQL', abbr:'SQL',
  category:'database',
  tier:2,
  year:1974,
  difficulty:2,
  color:'#336791',
  tagline:'Le langage universel pour interroger et manipuler les donnees relationnelles.',
  paradigms:['Declaratif','Ensembliste','Relationnel'],
  description:'SQL (Structured Query Language) est le langage standard pour communiquer avec les bases de donnees relationnelles. Plutot que de decrire comment obtenir les donnees, on declare ce que l\'on veut et le moteur optimise l\'execution. Il couvre l\'interrogation (SELECT), la manipulation (INSERT, UPDATE, DELETE), la definition de schema (CREATE, ALTER) et le controle transactionnel. Maitrise par PostgreSQL, MySQL, SQLite, Oracle et SQL Server, c\'est une competence incontournable pour tout developpeur backend ou data.',
  icon:'<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><ellipse cx="12" cy="5" rx="7" ry="3"/><path d="M5 5v6c0 1.66 3.13 3 7 3s7-1.34 7-3V5"/><path d="M5 11v6c0 1.66 3.13 3 7 3s7-1.34 7-3v-6"/><path d="M9 8.5l1.5 1.5L9 11.5"/><path d="M13.5 16h2"/></svg>',
  concepts:[
    {title:'SELECT, WHERE et ORDER BY', body:'SELECT choisit les colonnes a renvoyer, WHERE filtre les lignes selon des conditions, et ORDER BY trie le resultat. C\'est la base de toute interrogation declarative : on decrit le resultat voulu, pas la maniere de l\'obtenir.', code:'SELECT nom, age\nFROM clients\nWHERE age >= 18\nORDER BY age DESC;'},
    {title:'Les jointures (JOIN)', body:'Les jointures combinent les lignes de plusieurs tables sur une condition. INNER JOIN garde seulement les correspondances, LEFT JOIN garde toutes les lignes de gauche, RIGHT JOIN celles de droite, et FULL JOIN garde tout, comblant les manques par NULL.', code:'SELECT c.nom, co.total\nFROM clients c\nINNER JOIN commandes co ON co.client_id = c.id;'},
    {title:'Agregats et GROUP BY', body:'Les fonctions d\'agregat (COUNT, SUM, AVG, MIN, MAX) condensent plusieurs lignes en une valeur. GROUP BY regroupe les lignes par valeur commune avant d\'appliquer l\'agregat sur chaque groupe.', code:'SELECT pays, COUNT(*) AS nb\nFROM clients\nGROUP BY pays;'},
    {title:'HAVING vs WHERE', body:'WHERE filtre les lignes avant le regroupement, tandis que HAVING filtre les groupes apres l\'agregation. On utilise HAVING quand la condition porte sur un resultat d\'agregat comme COUNT ou SUM.', code:'SELECT pays, COUNT(*) AS nb\nFROM clients\nGROUP BY pays\nHAVING COUNT(*) > 10;'},
    {title:'Sous-requetes', body:'Une sous-requete est une requete imbriquee dans une autre. Elle peut alimenter un WHERE (avec IN ou un operateur de comparaison), apparaitre dans le SELECT, ou servir de table derivee dans le FROM.', code:'SELECT nom FROM clients\nWHERE id IN (\n  SELECT client_id FROM commandes\n  WHERE total > 100\n);'},
    {title:'Les index', body:'Un index est une structure (souvent un arbre B) qui accelere la recherche sur une ou plusieurs colonnes, evitant un parcours complet de la table. Ils accelerent les SELECT mais ralentissent legerement les ecritures et consomment de l\'espace disque.', code:'CREATE INDEX idx_clients_email\nON clients(email);'},
    {title:'Normalisation', body:'La normalisation organise les tables pour eliminer la redondance et les anomalies de mise a jour. Les formes normales (1NF, 2NF, 3NF) imposent l\'atomicite des valeurs, la dependance a la cle complete, puis l\'absence de dependances transitives.'},
    {title:'Transactions et ACID', body:'Une transaction regroupe plusieurs operations en une unite indivisible. Les proprietes ACID garantissent l\'Atomicite (tout ou rien), la Coherence (regles respectees), l\'Isolation (transactions independantes) et la Durabilite (resultats persistants).', code:'BEGIN;\nUPDATE comptes SET solde = solde - 100 WHERE id = 1;\nUPDATE comptes SET solde = solde + 100 WHERE id = 2;\nCOMMIT;'},
  ],
  example:{ lang:'sql', code:'-- Schema : clients et commandes\nCREATE TABLE clients (\n  id    INTEGER PRIMARY KEY,\n  nom   TEXT NOT NULL,\n  pays  TEXT\n);\n\nCREATE TABLE commandes (\n  id         INTEGER PRIMARY KEY,\n  client_id  INTEGER REFERENCES clients(id),\n  total      NUMERIC\n);\n\n-- Total depense par client, seulement ceux > 200,\n-- tries du plus gros au plus petit\nSELECT c.nom,\n       SUM(co.total) AS total_depense\nFROM clients c\nINNER JOIN commandes co ON co.client_id = c.id\nGROUP BY c.id, c.nom\nHAVING SUM(co.total) > 200\nORDER BY total_depense DESC;', explain:'On cree deux tables liees par une cle etrangere (client_id). La requete joint les commandes a leur client, regroupe par client, somme les totaux, ne garde que les clients ayant depense plus de 200, puis trie par montant decroissant. C\'est un cas typique combinant JOIN, agregat, GROUP BY et HAVING.' },
  roadmap:[
    {level:'Initie',goals:['Ecrire des SELECT avec WHERE et ORDER BY','Utiliser INSERT, UPDATE et DELETE','Comprendre les types de colonnes et les contraintes','Filtrer avec LIKE, IN et BETWEEN']},
    {level:'Adepte',goals:['Maitriser les quatre types de JOIN','Agreger avec GROUP BY et HAVING','Ecrire des sous-requetes correlees ou non','Gerer des transactions avec COMMIT et ROLLBACK']},
    {level:'Maitre',goals:['Concevoir un schema normalise (3NF)','Optimiser via index et plans d\'execution (EXPLAIN)','Utiliser les fonctions de fenetrage (OVER, PARTITION BY)','Comprendre les niveaux d\'isolation et les verrous']},
  ],
  resources:[
    {label:'SQLBolt - Tutoriel interactif',url:'https://sqlbolt.com/'},
    {label:'PostgreSQL - Documentation officielle',url:'https://www.postgresql.org/docs/'},
    {label:'Mode SQL Tutorial',url:'https://mode.com/sql-tutorial/'},
    {label:'Use The Index, Luke',url:'https://use-the-index-luke.com/'},
  ],
  games:{
    quiz:[
      {q:'Quelle clause filtre les lignes avant tout regroupement ?',options:['HAVING','WHERE','GROUP BY','ORDER BY'],answer:1,explain:'WHERE filtre les lignes individuelles avant l\'agregation ; HAVING agit apres, sur les groupes.'},
      {q:'Quel JOIN conserve toutes les lignes de la table de gauche meme sans correspondance ?',options:['INNER JOIN','LEFT JOIN','RIGHT JOIN','CROSS JOIN'],answer:1,explain:'LEFT JOIN garde toutes les lignes de gauche et remplit par NULL les colonnes de droite sans correspondance.'},
      {q:'Que signifie le D de ACID ?',options:['Determinisme','Durabilite','Dependance','Decomposition'],answer:1,explain:'La Durabilite garantit qu\'une transaction validee survit meme a une panne du systeme.'},
      {q:'Quelle fonction compte le nombre de lignes d\'un groupe ?',options:['SUM()','COUNT()','AVG()','TOTAL()'],answer:1,explain:'COUNT() renvoie le nombre de lignes ; COUNT(*) compte toutes les lignes du groupe.'},
      {q:'A quoi sert principalement un index ?',options:['Chiffrer les donnees','Accelerer les recherches','Sauvegarder la base','Trier l\'affichage'],answer:1,explain:'Un index accelere la recherche sur certaines colonnes en evitant un parcours complet de la table.'},
      {q:'Quelle forme normale exige des valeurs atomiques (indivisibles) ?',options:['1NF','2NF','3NF','BCNF'],answer:0,explain:'La premiere forme normale (1NF) impose que chaque colonne contienne une valeur atomique, pas de listes.'},
      {q:'Quel mot-cle annule une transaction en cours ?',options:['CANCEL','UNDO','ROLLBACK','REVERT'],answer:2,explain:'ROLLBACK annule toutes les modifications effectuees depuis le debut de la transaction.'},
      {q:'Ou place-t-on une condition portant sur un resultat de COUNT() ?',options:['Dans WHERE','Dans HAVING','Dans ORDER BY','Dans SELECT'],answer:1,explain:'HAVING filtre apres l\'agregation, donc c\'est la seule clause acceptant une condition sur COUNT().'},
    ],
    fillBlank:[
      {prompt:'Selectionner les noms tries par age decroissant.',code:'SELECT nom FROM clients ORDER BY age ___;',answer:'DESC',explain:'DESC trie en ordre decroissant ; ASC (par defaut) trie en ordre croissant.'},
      {prompt:'Joindre clients et commandes en gardant seulement les correspondances.',code:'SELECT * FROM clients c ___ JOIN commandes co ON co.client_id = c.id;',answer:'INNER',explain:'INNER JOIN ne renvoie que les lignes ayant une correspondance dans les deux tables.'},
      {prompt:'Compter les clients par pays.',code:'SELECT pays, COUNT(*) FROM clients ___ BY pays;',answer:'GROUP',explain:'GROUP BY regroupe les lignes par valeur commune avant d\'appliquer l\'agregat.'},
      {prompt:'Ne garder que les groupes de plus de 5 elements.',code:'SELECT pays, COUNT(*) FROM clients GROUP BY pays ___ COUNT(*) > 5;',answer:'HAVING',explain:'HAVING filtre les groupes apres l\'agregation, contrairement a WHERE.'},
      {prompt:'Creer un index sur la colonne email.',code:'CREATE ___ idx_email ON clients(email);',answer:'INDEX',explain:'CREATE INDEX cree une structure d\'acceleration sur la ou les colonnes indiquees.'},
      {prompt:'Valider definitivement une transaction.',code:'BEGIN; UPDATE comptes SET solde = 0; ___;',answer:'COMMIT',explain:'COMMIT valide et rend permanentes toutes les modifications de la transaction.'},
    ],
    bugHunt:[
      {code:'SELECT nom, COUNT(*)\nFROM clients\nWHERE COUNT(*) > 3\nGROUP BY nom;',buggyLine:3,explain:'On ne peut pas utiliser un agregat dans WHERE ; il faut filtrer les groupes avec HAVING apres GROUP BY.',fix:'SELECT nom, COUNT(*)\nFROM clients\nGROUP BY nom\nHAVING COUNT(*) > 3;'},
      {code:'SELECT c.nom, co.total\nFROM clients c\nINNER JOIN commandes co\nWHERE co.client_id = c.id;',buggyLine:4,explain:'Un JOIN exige une clause ON pour la condition de jointure ; WHERE seul ne lie pas correctement les tables.',fix:'SELECT c.nom, co.total\nFROM clients c\nINNER JOIN commandes co\nON co.client_id = c.id;'},
      {code:'SELECT nom, age\nFROM clients\nORDER BY age\nWHERE age > 18;',buggyLine:4,explain:'L\'ordre des clauses est invalide : WHERE doit preceder ORDER BY, qui termine toujours la requete.',fix:'SELECT nom, age\nFROM clients\nWHERE age > 18\nORDER BY age;'},
      {code:'INSERT INTO clients (nom, pays)\nVALUES (\'Alice\');',buggyLine:2,explain:'Le nombre de valeurs ne correspond pas aux colonnes : deux colonnes sont nommees mais une seule valeur est fournie.',fix:'INSERT INTO clients (nom, pays)\nVALUES (\'Alice\', \'France\');'},
    ],
    output:[
      {code:'-- clients : (1,A,FR) (2,B,FR) (3,C,US)\nSELECT pays, COUNT(*)\nFROM clients\nGROUP BY pays;',options:['FR 3','FR 2 / US 1','FR 1 / US 1'],answer:1,explain:'Deux clients sont en FR et un en US, donc le regroupement donne FR=2 et US=1.'},
      {code:'-- clients ages : 30, 20, 40\nSELECT MAX(age) FROM clients;',options:['20','30','40'],answer:2,explain:'MAX renvoie la plus grande valeur de la colonne, soit 40.'},
      {code:'-- totaux commandes : 100, 50, 50\nSELECT SUM(total) FROM commandes;',options:['150','200','100'],answer:1,explain:'SUM additionne toutes les valeurs : 100 + 50 + 50 = 200.'},
      {code:'-- clients : Bob, Alice, Carol\nSELECT nom FROM clients\nORDER BY nom\nLIMIT 1;',options:['Bob','Alice','Carol'],answer:1,explain:'ORDER BY nom trie alphabetiquement (Alice, Bob, Carol) et LIMIT 1 garde la premiere : Alice.'},
      {code:'-- commandes : 5 lignes au total\nSELECT COUNT(*) FROM commandes;',options:['1','5','0'],answer:1,explain:'COUNT(*) compte toutes les lignes de la table, soit 5.'},
    ],
  },
});
